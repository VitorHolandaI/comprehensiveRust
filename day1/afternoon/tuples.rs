fn main() {
    let t: (i8, bool) = (7, true);// can have diferent types!!!!
                                  // same way a array cant add or remove! cant loop in this also
                                  // since need to have same type all elemnts!
    dbg!(t.0);
    dbg!(t.1);
}
